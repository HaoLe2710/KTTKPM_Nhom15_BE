package fit.iuh.kttkpm_nhom15_be.auth.domain.repositories;

public interface AuthRepository {
    // Hiện tại mình dùng JWT nên chưa cần lưu DB ở module Auth,
    // nhưng vẫn tạo interface để đúng cấu trúc Clean.
}